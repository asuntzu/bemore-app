# Required
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Optional — defaults shown
POLL_INTERVAL_MINUTES=15
DIGEST_HOUR=8        # Hour (UTC) for daily digest delivery

# Paths — override for Railway volume mount
DB_DIR=/app/data
LOG_DIR=/app/logs
