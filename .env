# Be More Inc. Frontend — environment variables
# Copy to .env and fill in real values before running

# API base URL — use /api in dev (Vite proxy forwards to localhost:3001)
# In production set to your deployed API URL e.g. https://api.bemoreinc.com
VITE_API_BASE_URL=/api
